global:
  scrape_interval: 5s
  scrape_timeout: 3s

scrape_configs:
  - job_name: "nio-http-server"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "host.docker.internal:8080"
